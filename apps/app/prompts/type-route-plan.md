# Routed views

A prior read identified the views below. Write one planning block per `TYPE`
line, in order, including repeated types. Enrich each view without merging,
dropping or changing its type. The original text still decides its content.
