# Class diagram details

Use `class Name { ... }` with one field or method per line. Use `+`, `-`, or `#`
for visibility and `<|--` for inheritance. Keep class IDs distinct from
reserved Mermaid words and quote labels when punctuation requires it.
