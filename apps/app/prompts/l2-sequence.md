# Sequence diagram details

Declare participants before messages. Use `->>` for a request and `-->>` for a
reply. Use `Note over A,B: text` for phase context; do not use `== Phase ==`,
which is not supported by the canvas converter. Use `loop` or `alt` only when
the description actually repeats or branches.
