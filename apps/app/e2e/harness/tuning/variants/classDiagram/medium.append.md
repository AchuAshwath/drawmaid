## Class readability experiment

When the source provides members, preserve their names and types and make
spoken visibility explicit with Mermaid's `+`, `-` or `#`. Keep one member per
line and show a stated inheritance, ownership or association distinction;
leave unspecified details alone. Use grouping only when the source names a
package, layer or other real shared boundary.
